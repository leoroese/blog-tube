import { ApolloServer } from 'apollo-server';

describe('Create book Mutation integration tests', () => {
  const apolloServer = new ApolloServer();
  beforeAll(() => {});

  it('should create and return created book', () => {});
});
